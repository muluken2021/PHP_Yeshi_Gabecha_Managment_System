openapi: 3.0.3
info:
  title: Yeshih Gabcha Event Management API
  description: |
    Complete API documentation for the Yeshih Gabcha Event Management System.
    This API handles authentication, bookings, events, payments, gallery, services, and admin operations.
  version: 1.0.0
  contact:
    name: API Support
    email: support@yeshih-gabcha-events.com

servers:
  - url: http://localhost/api
    description: Local development server
  - url: https://api.yeshih-gabcha-events.com/api
    description: Production server

tags:
  - name: Authentication
    description: User authentication and account management
  - name: Bookings
    description: Event booking management
  - name: Events
    description: Event management and ticket purchasing
  - name: Services
    description: Service catalog management
  - name: Gallery
    description: Gallery and photo management
  - name: Payments
    description: Payment processing and verification
  - name: Users
    description: User profile and account management
  - name: Dashboard
    description: Dashboard metrics and statistics
  - name: Reports
    description: Admin reports and analytics
  - name: Admin Config
    description: Admin configuration for pricing and payment methods
  - name: Notifications
    description: Admin notification management

components:
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: JWT token obtained from login/register endpoints

  schemas:

    # ─── Standard Response Wrapper ───────────────────────────────────────────────
    SuccessResponse:
      type: object
      properties:
        success:
          type: boolean
          example: true
        message:
          type: string
          example: Operation successful
        data:
          type: object

    ErrorResponse:
      type: object
      properties:
        success:
          type: boolean
          example: false
        message:
          type: string
          example: An error occurred
        errors:
          type: array
          items:
            type: object
            properties:
              field:
                type: string
              message:
                type: string

    # ─── User ────────────────────────────────────────────────────────────────────
    User:
      type: object
      properties:
        id:
          type: integer
          example: 1
        firstName:
          type: string
          example: Abebe
        lastName:
          type: string
          example: Kebede
        email:
          type: string
          format: email
          example: abebe@example.com
        phone:
          type: string
          example: "0911223344"
        role:
          type: string
          enum: [user, admin]
          example: user
        status:
          type: string
          enum: [active, inactive, pending, suspended]
          example: active
        profileImage:
          type: string
          nullable: true
          example: /uploads/profiles/user1.jpg
        emailVerified:
          type: boolean
          example: false
        twoFactorEnabled:
          type: boolean
          example: false
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    # ─── Booking ─────────────────────────────────────────────────────────────────
    Booking:
      type: object
      properties:
        id:
          type: integer
          example: 1
        userId:
          type: integer
          example: 1
        customerName:
          type: string
          example: Abebe Kebede
        customerEmail:
          type: string
          format: email
          example: abebe@example.com
        customerPhone:
          type: string
          example: "0911223344"
        eventType:
          type: string
          enum: [wedding, birthday, corporate, other]
          example: wedding
        eventDate:
          type: string
          format: date
          example: "2026-06-15"
        eventTime:
          type: string
          example: "14:00"
        guestCount:
          type: integer
          example: 150
        durationHours:
          type: integer
          example: 5
        message:
          type: string
          nullable: true
          example: Please arrange floral decorations
        status:
          type: string
          enum: [pending, confirmed, cancelled, completed]
          example: pending
        totalPrice:
          type: number
          format: float
          example: 25000.00
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    # ─── Event ───────────────────────────────────────────────────────────────────
    Event:
      type: object
      properties:
        id:
          type: integer
          example: 1
        title:
          type: string
          example: Ethiopian New Year Celebration
        description:
          type: string
          nullable: true
          example: Join us for a grand celebration
        eventType:
          type: string
          example: cultural
        location:
          type: string
          nullable: true
          example: Addis Ababa, Ethiopia
        latitude:
          type: number
          format: float
          nullable: true
          example: 9.0192
        longitude:
          type: number
          format: float
          nullable: true
          example: 38.7525
        eventDate:
          type: string
          format: date
          example: "2026-09-11"
        eventTime:
          type: string
          example: "18:00"
        ticketPrice:
          type: number
          format: float
          example: 500.00
        totalTickets:
          type: integer
          nullable: true
          example: 200
        remainingTickets:
          type: integer
          nullable: true
          example: 150
        imageUrl:
          type: string
          nullable: true
          example: /uploads/events/event1.jpg
        status:
          type: string
          enum: [active, inactive, cancelled]
          example: active
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    # ─── Service ─────────────────────────────────────────────────────────────────
    Service:
      type: object
      properties:
        id:
          type: integer
          example: 1
        name:
          type: string
          example: Wedding Photography
        description:
          type: string
          nullable: true
          example: Professional wedding photography service
        price:
          type: number
          format: float
          example: 15000.00
        category:
          type: string
          example: photography
        status:
          type: string
          enum: [active, inactive]
          example: active
        featured:
          type: boolean
          example: true
        images:
          type: array
          items:
            type: string
          example: ["/uploads/services/img1.jpg", "/uploads/services/img2.jpg"]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    # ─── Gallery ─────────────────────────────────────────────────────────────────
    GalleryItem:
      type: object
      properties:
        id:
          type: integer
          example: 1
        title:
          type: string
          example: Wedding at Sheraton
        description:
          type: string
          nullable: true
          example: Beautiful wedding ceremony
        category:
          type: string
          example: wedding
        location:
          type: string
          nullable: true
          example: Sheraton Addis
        date:
          type: string
          format: date
          nullable: true
          example: "2026-03-20"
        imageUrl:
          type: string
          example: /uploads/gallery/photo1.jpg
        reactions:
          type: object
          additionalProperties:
            type: integer
          example: {"like": 12, "love": 5}
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    # ─── Payment ─────────────────────────────────────────────────────────────────
    Payment:
      type: object
      properties:
        id:
          type: integer
          example: 1
        userId:
          type: integer
          example: 1
        bookingId:
          type: integer
          nullable: true
          example: 1
        eventId:
          type: integer
          nullable: true
          example: null
        amount:
          type: number
          format: float
          example: 25000.00
        method:
          type: string
          enum: [telebirr, cbe, commercial, abyssinia]
          example: telebirr
        status:
          type: string
          enum: [pending, approved, rejected]
          example: pending
        proofUrl:
          type: string
          nullable: true
          example: /uploads/proofs/proof1.jpg
        transactionId:
          type: string
          nullable: true
          example: TXN123456
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    # ─── Pricing Rule ─────────────────────────────────────────────────────────────
    PricingRule:
      type: object
      properties:
        eventType:
          type: string
          enum: [wedding, birthday, corporate, other]
          example: wedding
        basePrice:
          type: number
          format: float
          example: 10000.00
        perGuest:
          type: number
          format: float
          example: 50.00
        perHour:
          type: number
          format: float
          example: 500.00
        defaultHours:
          type: integer
          example: 5

    # ─── Payment Method Config ────────────────────────────────────────────────────
    PaymentMethodConfig:
      type: object
      properties:
        method:
          type: string
          enum: [telebirr, cbe, commercial, abyssinia]
          example: telebirr
        receiverName:
          type: string
          example: Habesha Events
        receiverPhone:
          type: string
          example: "0911000000"
        receiverAccountNumber:
          type: string
          example: "1000123456789"
        note:
          type: string
          nullable: true
          example: Please include your booking ID in the reference
        active:
          type: boolean
          example: true

    # ─── Notification ─────────────────────────────────────────────────────────────
    Notification:
      type: object
      properties:
        id:
          type: integer
          example: 1
        title:
          type: string
          example: New Booking Received
        message:
          type: string
          example: A new booking has been submitted by Abebe Kebede
        type:
          type: string
          example: booking
        isRead:
          type: boolean
          example: false
        createdAt:
          type: string
          format: date-time

    # ─── Payment Initiation Response ─────────────────────────────────────────────
    PaymentInitResponse:
      type: object
      properties:
        payment:
          $ref: '#/components/schemas/Payment'
        receiver:
          type: object
          properties:
            receiverName:
              type: string
              example: Habesha Events
            receiverPhone:
              type: string
              example: "0911000000"
            receiverAccountNumber:
              type: string
              example: "1000123456789"
            note:
              type: string
              nullable: true
        instructions:
          type: object
          properties:
            title:
              type: string
              example: How to pay via Telebirr
            steps:
              type: array
              items:
                type: string
              example:
                - Open your Telebirr app
                - Send to the number above
                - Take a screenshot of the confirmation
                - Upload the screenshot below
            note:
              type: string
              nullable: true
              example: Payment must be completed within 24 hours


paths:
  # ═══════════════════════════════════════════════════════════════════════════════
  # AUTHENTICATION
  # ═══════════════════════════════════════════════════════════════════════════════
  
  /auth/register:
    post:
      tags:
        - Authentication
      summary: Register a new user
      description: Create a new user account and receive JWT token
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - firstName
                - lastName
                - email
                - phone
                - password
              properties:
                firstName:
                  type: string
                  example: Abebe
                lastName:
                  type: string
                  example: Kebede
                email:
                  type: string
                  format: email
                  example: abebe@example.com
                phone:
                  type: string
                  example: "0911223344"
                password:
                  type: string
                  format: password
                  minLength: 8
                  example: SecurePass123
      responses:
        '200':
          description: Registration successful
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: Registration successful
                  data:
                    type: object
                    properties:
                      accessToken:
                        type: string
                        example: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
                      user:
                        $ref: '#/components/schemas/User'
        '400':
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /auth/login:
    post:
      tags:
        - Authentication
      summary: Login user
      description: Authenticate user and receive JWT token. Returns 2FA challenge if enabled.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - identifier
                - password
              properties:
                identifier:
                  type: string
                  description: Email or phone number
                  example: abebe@example.com
                password:
                  type: string
                  format: password
                  example: SecurePass123
      responses:
        '200':
          description: Login successful
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: Login successful
                  data:
                    type: object
                    properties:
                      accessToken:
                        type: string
                        example: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
                      user:
                        $ref: '#/components/schemas/User'
        '202':
          description: 2FA required
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: false
                  data:
                    type: object
                    properties:
                      twoFactorRequired:
                        type: boolean
                        example: true
                      twoFactorToken:
                        type: string
                        example: temp_token_for_2fa_verification
        '401':
          description: Invalid credentials
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /auth/2fa/verify:
    post:
      tags:
        - Authentication
      summary: Verify 2FA OTP
      description: Complete login by verifying 2FA OTP code
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - twoFactorToken
                - otp
              properties:
                twoFactorToken:
                  type: string
                  example: temp_token_for_2fa_verification
                otp:
                  type: string
                  example: "123456"
      responses:
        '200':
          description: 2FA verification successful
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: object
                    properties:
                      accessToken:
                        type: string
                      user:
                        $ref: '#/components/schemas/User'
        '401':
          description: Invalid OTP
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /auth/2fa/enable:
    post:
      tags:
        - Authentication
      summary: Enable 2FA
      description: Enable two-factor authentication for the current user
      security:
        - BearerAuth: []
      responses:
        '200':
          description: 2FA enabled successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: Two-factor authentication enabled
                  data:
                    type: object
                    properties:
                      qrCode:
                        type: string
                        description: Base64 encoded QR code image
                      secret:
                        type: string
                        example: JBSWY3DPEHPK3PXP

  /auth/2fa/disable:
    post:
      tags:
        - Authentication
      summary: Disable 2FA
      description: Disable two-factor authentication for the current user
      security:
        - BearerAuth: []
      responses:
        '200':
          description: 2FA disabled successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse'

  /auth/change-password:
    post:
      tags:
        - Authentication
      summary: Change password
      description: Change the current user's password
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - currentPassword
                - newPassword
              properties:
                currentPassword:
                  type: string
                  format: password
                  example: OldPass123
                newPassword:
                  type: string
                  format: password
                  minLength: 8
                  example: NewSecurePass456
      responses:
        '200':
          description: Password changed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse'
        '401':
          description: Current password is incorrect
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /auth/resend-verification:
    post:
      tags:
        - Authentication
      summary: Resend email verification
      description: Resend email verification link to the current user
      security:
        - BearerAuth: []
      responses:
        '200':
          description: Verification email sent
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse'

  /auth/verify-email:
    get:
      tags:
        - Authentication
      summary: Verify email address
      description: Verify user's email address using token from email
      parameters:
        - name: token
          in: query
          required: true
          schema:
            type: string
          description: Email verification token
      responses:
        '200':
          description: Email verified successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse'
        '400':
          description: Invalid or expired token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'


  # ═══════════════════════════════════════════════════════════════════════════════
  # USERS
  # ═══════════════════════════════════════════════════════════════════════════════

  /users/me:
    get:
      tags:
        - Users
      summary: Get current user
      description: Get the currently authenticated user's full profile
      security:
        - BearerAuth: []
      responses:
        '200':
          description: User profile retrieved
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    $ref: '#/components/schemas/User'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    put:
      tags:
        - Users
      summary: Update current user profile
      description: Update the currently authenticated user's profile data
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                firstName:
                  type: string
                  example: Abebe
                lastName:
                  type: string
                  example: Kebede
                phone:
                  type: string
                  example: "0911223344"
      responses:
        '200':
          description: Profile updated successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    $ref: '#/components/schemas/User'

  /users/me/profile-image-upload:
    put:
      tags:
        - Users
      summary: Upload profile image
      description: Upload a new profile picture for the current user
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - profileImage
              properties:
                profileImage:
                  type: string
                  format: binary
                  description: Profile image file (JPEG, PNG, max 5MB)
      responses:
        '200':
          description: Profile image uploaded successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: object
                    properties:
                      profileImage:
                        type: string
                        example: /uploads/profiles/user1.jpg

  /users/profile:
    get:
      tags:
        - Users
      summary: Get user profile
      description: Alias for GET /users/me
      security:
        - BearerAuth: []
      responses:
        '200':
          description: User profile retrieved
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    $ref: '#/components/schemas/User'
    put:
      tags:
        - Users
      summary: Update user profile
      description: Alias for PUT /users/me
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                firstName:
                  type: string
                lastName:
                  type: string
                phone:
                  type: string
      responses:
        '200':
          description: Profile updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    $ref: '#/components/schemas/User'

  /users/change-password:
    put:
      tags:
        - Users
      summary: Change password (user route)
      description: Change password via user profile settings
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - currentPassword
                - newPassword
              properties:
                currentPassword:
                  type: string
                  format: password
                newPassword:
                  type: string
                  format: password
                  minLength: 8
      responses:
        '200':
          description: Password changed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse'

  /users/deactivate:
    put:
      tags:
        - Users
      summary: Deactivate account
      description: Deactivate the current user's account
      security:
        - BearerAuth: []
      responses:
        '200':
          description: Account deactivated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse'

  /users/admin/users:
    get:
      tags:
        - Users
      summary: List all users (Admin)
      description: Get paginated list of all users. Admin only.
      security:
        - BearerAuth: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
            example: 200
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
        - name: role
          in: query
          schema:
            type: string
            enum: [user, admin]
        - name: status
          in: query
          schema:
            type: string
            enum: [active, inactive, pending, suspended]
        - name: search
          in: query
          schema:
            type: string
      responses:
        '200':
          description: Users list retrieved
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: object
                    properties:
                      users:
                        type: array
                        items:
                          $ref: '#/components/schemas/User'
                      total:
                        type: integer
                        example: 50

  /users/admin/users/{id}:
    put:
      tags:
        - Users
      summary: Update user status (Admin)
      description: Update a user's status or role. Admin only.
      security:
        - BearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          example: 1
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                status:
                  type: string
                  enum: [active, inactive, pending, suspended]
                  example: inactive
                role:
                  type: string
                  enum: [user, admin]
      responses:
        '200':
          description: User updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    $ref: '#/components/schemas/User'


  # ═══════════════════════════════════════════════════════════════════════════════
  # BOOKINGS
  # ═══════════════════════════════════════════════════════════════════════════════

  /bookings/calc-price:
    post:
      tags:
        - Bookings
      summary: Calculate booking price
      description: Calculate the total price for a booking before creating it
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - eventType
                - guestCount
                - durationHours
                - eventDate
                - eventTime
              properties:
                eventType:
                  type: string
                  enum: [wedding, birthday, corporate, other]
                  example: wedding
                guestCount:
                  type: integer
                  example: 150
                durationHours:
                  type: integer
                  example: 5
                eventDate:
                  type: string
                  format: date
                  example: "2026-06-15"
                eventTime:
                  type: string
                  example: "14:00"
      responses:
        '200':
          description: Price calculated
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: object
                    properties:
                      totalPrice:
                        type: number
                        example: 25000.00
                      basePrice:
                        type: number
                        example: 10000.00
                      perGuest:
                        type: number
                        example: 50.00
                      perHour:
                        type: number
                        example: 500.00
                      durationHours:
                        type: integer
                        example: 5

  /bookings:
    post:
      tags:
        - Bookings
      summary: Create a booking
      description: Create a new event booking. Requires authentication.
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - customerName
                - customerEmail
                - customerPhone
                - eventType
                - eventDate
                - eventTime
                - guestCount
                - durationHours
              properties:
                customerName:
                  type: string
                  example: Abebe Kebede
                customerEmail:
                  type: string
                  format: email
                  example: abebe@example.com
                customerPhone:
                  type: string
                  example: "0911223344"
                eventType:
                  type: string
                  enum: [wedding, birthday, corporate, other]
                  example: wedding
                eventDate:
                  type: string
                  format: date
                  example: "2026-06-15"
                eventTime:
                  type: string
                  example: "14:00"
                guestCount:
                  type: integer
                  example: 150
                durationHours:
                  type: integer
                  example: 5
                message:
                  type: string
                  nullable: true
                  example: Please arrange floral decorations
      responses:
        '201':
          description: Booking created successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    $ref: '#/components/schemas/Booking'
        '400':
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /bookings/my-bookings:
    get:
      tags:
        - Bookings
      summary: Get my bookings
      description: Get all bookings for the currently authenticated user
      security:
        - BearerAuth: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
        - name: status
          in: query
          schema:
            type: string
            enum: [pending, confirmed, cancelled, completed]
      responses:
        '200':
          description: Bookings retrieved
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: object
                    properties:
                      bookings:
                        type: array
                        items:
                          $ref: '#/components/schemas/Booking'
                      total:
                        type: integer
                        example: 5

  /bookings/{id}:
    get:
      tags:
        - Bookings
      summary: Get booking by ID
      description: Get a specific booking by its ID
      security:
        - BearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          example: 1
      responses:
        '200':
          description: Booking retrieved
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    $ref: '#/components/schemas/Booking'
        '404':
          description: Booking not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /bookings/{id}/proceed-payment:
    post:
      tags:
        - Bookings
      summary: Initiate booking payment
      description: Initiate payment for a booking. Returns receiver info and payment instructions.
      security:
        - BearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          example: 1
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - paymentMethod
              properties:
                paymentMethod:
                  type: string
                  enum: [telebirr, cbe, commercial, abyssinia]
                  example: telebirr
                phoneNumber:
                  type: string
                  nullable: true
                  description: Required for telebirr and cbe methods
                  example: "0911223344"
      responses:
        '200':
          description: Payment initiated
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    $ref: '#/components/schemas/PaymentInitResponse'

  /bookings/{id}/qr:
    get:
      tags:
        - Bookings
      summary: Get booking QR code
      description: Get QR code for a confirmed booking (only available after payment approval)
      security:
        - BearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          example: 1
      responses:
        '200':
          description: QR code retrieved
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: object
                    properties:
                      qrCode:
                        type: string
                        description: Base64 encoded QR code image
                      bookingId:
                        type: integer
                        example: 1

  /bookings/admin/bookings:
    get:
      tags:
        - Bookings
      summary: List all bookings (Admin)
      description: Get all bookings with filtering and pagination. Admin only.
      security:
        - BearerAuth: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
        - name: status
          in: query
          schema:
            type: string
            enum: [pending, confirmed, cancelled, completed]
        - name: eventType
          in: query
          schema:
            type: string
            enum: [wedding, birthday, corporate, other]
        - name: search
          in: query
          schema:
            type: string
            description: Search by customer name, email, or phone
      responses:
        '200':
          description: Bookings list retrieved
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: object
                    properties:
                      bookings:
                        type: array
                        items:
                          $ref: '#/components/schemas/Booking'
                      total:
                        type: integer
                        example: 100

  /bookings/admin/bookings/{id}/status:
    put:
      tags:
        - Bookings
      summary: Update booking status (Admin)
      description: Update the status of a booking. Admin only.
      security:
        - BearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          example: 1
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - status
              properties:
                status:
                  type: string
                  enum: [pending, confirmed, cancelled, completed]
                  example: confirmed
      responses:
        '200':
          description: Booking status updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    $ref: '#/components/schemas/Booking'


  # ═══════════════════════════════════════════════════════════════════════════════
  # EVENTS
  # ═══════════════════════════════════════════════════════════════════════════════

  /events:
    get:
      tags:
        - Events
      summary: List events
      description: Get all public events with optional filtering
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
        - name: status
          in: query
          schema:
            type: string
            enum: [active, inactive, cancelled]
            default: active
        - name: eventType
          in: query
          schema:
            type: string
        - name: search
          in: query
          schema:
            type: string
      responses:
        '200':
          description: Events list retrieved
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: object
                    properties:
                      events:
                        type: array
                        items:
                          $ref: '#/components/schemas/Event'
                      total:
                        type: integer
                        example: 10
    post:
      tags:
        - Events
      summary: Create event (Admin)
      description: Create a new event. Admin only.
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - title
                - eventType
                - eventDate
                - eventTime
                - ticketPrice
              properties:
                title:
                  type: string
                  example: Ethiopian New Year Celebration
                description:
                  type: string
                  example: Join us for a grand celebration
                eventType:
                  type: string
                  example: cultural
                location:
                  type: string
                  example: Addis Ababa, Ethiopia
                latitude:
                  type: number
                  format: float
                  example: 9.0192
                longitude:
                  type: number
                  format: float
                  example: 38.7525
                eventDate:
                  type: string
                  format: date
                  example: "2026-09-11"
                eventTime:
                  type: string
                  example: "18:00"
                ticketPrice:
                  type: number
                  format: float
                  example: 500.00
                totalTickets:
                  type: integer
                  example: 200
                status:
                  type: string
                  enum: [active, inactive]
                  default: active
                image:
                  type: string
                  format: binary
                  description: Event image file (JPEG, PNG, max 10MB)
      responses:
        '201':
          description: Event created
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    $ref: '#/components/schemas/Event'

  /events/{id}:
    get:
      tags:
        - Events
      summary: Get event by ID
      description: Get a specific event's details
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          example: 1
      responses:
        '200':
          description: Event retrieved
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    $ref: '#/components/schemas/Event'
        '404':
          description: Event not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    put:
      tags:
        - Events
      summary: Update event (Admin)
      description: Update an existing event. Admin only.
      security:
        - BearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          example: 1
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                title:
                  type: string
                description:
                  type: string
                eventType:
                  type: string
                location:
                  type: string
                latitude:
                  type: number
                longitude:
                  type: number
                eventDate:
                  type: string
                  format: date
                eventTime:
                  type: string
                ticketPrice:
                  type: number
                totalTickets:
                  type: integer
                status:
                  type: string
                  enum: [active, inactive, cancelled]
                image:
                  type: string
                  format: binary
          application/json:
            schema:
              type: object
              properties:
                title:
                  type: string
                description:
                  type: string
                eventType:
                  type: string
                location:
                  type: string
                eventDate:
                  type: string
                  format: date
                eventTime:
                  type: string
                ticketPrice:
                  type: number
                totalTickets:
                  type: integer
                status:
                  type: string
                  enum: [active, inactive, cancelled]
      responses:
        '200':
          description: Event updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    $ref: '#/components/schemas/Event'
    delete:
      tags:
        - Events
      summary: Delete event (Admin)
      description: Delete an event. Admin only.
      security:
        - BearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          example: 1
      responses:
        '200':
          description: Event deleted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse'

  /events/{id}/proceed-payment:
    post:
      tags:
        - Events
      summary: Buy event ticket
      description: Initiate payment to purchase a ticket for an event
      security:
        - BearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          example: 1
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - paymentMethod
              properties:
                paymentMethod:
                  type: string
                  enum: [telebirr, cbe, commercial, abyssinia]
                  example: telebirr
                phoneNumber:
                  type: string
                  nullable: true
                  description: Required for telebirr and cbe
                  example: "0911223344"
      responses:
        '200':
          description: Payment initiated
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    $ref: '#/components/schemas/PaymentInitResponse'


  # ═══════════════════════════════════════════════════════════════════════════════
  # SERVICES
  # ═══════════════════════════════════════════════════════════════════════════════

  /services:
    get:
      tags:
        - Services
      summary: List services
      description: Get all services with optional filtering
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
        - name: status
          in: query
          schema:
            type: string
            enum: [active, inactive]
        - name: category
          in: query
          schema:
            type: string
        - name: featured
          in: query
          schema:
            type: boolean
      responses:
        '200':
          description: Services list retrieved
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: object
                    properties:
                      services:
                        type: array
                        items:
                          $ref: '#/components/schemas/Service'
                      total:
                        type: integer
                        example: 15
    post:
      tags:
        - Services
      summary: Create service (Admin)
      description: Create a new service. Admin only.
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - name
                - price
                - category
              properties:
                name:
                  type: string
                  example: Wedding Photography
                description:
                  type: string
                  example: Professional wedding photography service
                price:
                  type: number
                  format: float
                  example: 15000.00
                category:
                  type: string
                  example: photography
                status:
                  type: string
                  enum: [active, inactive]
                  default: active
                featured:
                  type: boolean
                  default: false
                images:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Service images (multiple files allowed)
      responses:
        '201':
          description: Service created
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    $ref: '#/components/schemas/Service'

  /services/{id}:
    get:
      tags:
        - Services
      summary: Get service by ID
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          example: 1
      responses:
        '200':
          description: Service retrieved
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    $ref: '#/components/schemas/Service'
        '404':
          description: Service not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    put:
      tags:
        - Services
      summary: Update service (Admin)
      security:
        - BearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          example: 1
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                name:
                  type: string
                description:
                  type: string
                price:
                  type: number
                category:
                  type: string
                status:
                  type: string
                  enum: [active, inactive]
                featured:
                  type: boolean
                images:
                  type: array
                  items:
                    type: string
                    format: binary
      responses:
        '200':
          description: Service updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    $ref: '#/components/schemas/Service'
    delete:
      tags:
        - Services
      summary: Delete service (Admin)
      security:
        - BearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          example: 1
      responses:
        '200':
          description: Service deleted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse'

  /services/{id}/images/{imageIndex}:
    delete:
      tags:
        - Services
      summary: Delete service image (Admin)
      description: Delete a specific image from a service by its index
      security:
        - BearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          example: 1
        - name: imageIndex
          in: path
          required: true
          schema:
            type: integer
          description: Zero-based index of the image to delete
          example: 0
      responses:
        '200':
          description: Image deleted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse'


  # ═══════════════════════════════════════════════════════════════════════════════
  # GALLERY
  # ═══════════════════════════════════════════════════════════════════════════════

  /gallery:
    get:
      tags:
        - Gallery
      summary: List gallery items
      description: Get all gallery items with optional filtering
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
        - name: category
          in: query
          schema:
            type: string
        - name: search
          in: query
          schema:
            type: string
      responses:
        '200':
          description: Gallery items retrieved
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: object
                    properties:
                      gallery:
                        type: array
                        items:
                          $ref: '#/components/schemas/GalleryItem'
                      total:
                        type: integer
                        example: 50
    post:
      tags:
        - Gallery
      summary: Create gallery item (Admin)
      description: Upload a new gallery item. Admin only.
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - title
                - category
                - image
              properties:
                title:
                  type: string
                  example: Wedding at Sheraton
                description:
                  type: string
                  example: Beautiful wedding ceremony
                category:
                  type: string
                  example: wedding
                location:
                  type: string
                  example: Sheraton Addis
                date:
                  type: string
                  format: date
                  example: "2026-03-20"
                image:
                  type: string
                  format: binary
                  description: Gallery image file (JPEG, PNG, max 10MB)
      responses:
        '201':
          description: Gallery item created
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    $ref: '#/components/schemas/GalleryItem'

  /gallery/{id}:
    get:
      tags:
        - Gallery
      summary: Get gallery item by ID
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          example: 1
      responses:
        '200':
          description: Gallery item retrieved
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    $ref: '#/components/schemas/GalleryItem'
        '404':
          description: Gallery item not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    put:
      tags:
        - Gallery
      summary: Update gallery item (Admin)
      security:
        - BearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          example: 1
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                title:
                  type: string
                description:
                  type: string
                category:
                  type: string
                location:
                  type: string
                date:
                  type: string
                  format: date
      responses:
        '200':
          description: Gallery item updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    $ref: '#/components/schemas/GalleryItem'
    delete:
      tags:
        - Gallery
      summary: Delete gallery item (Admin)
      security:
        - BearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          example: 1
      responses:
        '200':
          description: Gallery item deleted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse'

  /gallery/{id}/reaction:
    post:
      tags:
        - Gallery
      summary: Add reaction to gallery item
      description: Add or update a reaction (like, love, etc.) to a gallery item
      security:
        - BearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          example: 1
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - reaction
              properties:
                reaction:
                  type: string
                  enum: [like, love, wow, sad, angry]
                  example: like
      responses:
        '200':
          description: Reaction added
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: object
                    properties:
                      reactions:
                        type: object
                        additionalProperties:
                          type: integer
                        example: {"like": 13, "love": 5}


  # ═══════════════════════════════════════════════════════════════════════════════
  # PAYMENTS
  # ═══════════════════════════════════════════════════════════════════════════════

  /payments/my-payments:
    get:
      tags:
        - Payments
      summary: Get my payments
      description: Get all payments for the currently authenticated user
      security:
        - BearerAuth: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
        - name: status
          in: query
          schema:
            type: string
            enum: [pending, approved, rejected]
      responses:
        '200':
          description: Payments retrieved
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: object
                    properties:
                      payments:
                        type: array
                        items:
                          $ref: '#/components/schemas/Payment'
                      total:
                        type: integer
                        example: 5

  /payments/{id}:
    get:
      tags:
        - Payments
      summary: Get payment by ID
      security:
        - BearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          example: 1
      responses:
        '200':
          description: Payment retrieved
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    $ref: '#/components/schemas/Payment'
        '404':
          description: Payment not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /payments/{id}/proof:
    post:
      tags:
        - Payments
      summary: Upload payment proof
      description: Upload a screenshot or proof of payment
      security:
        - BearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          example: 1
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - proof
              properties:
                proof:
                  type: string
                  format: binary
                  description: Payment proof image (JPEG, PNG, max 5MB)
      responses:
        '200':
          description: Proof uploaded successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: object
                    properties:
                      proofUrl:
                        type: string
                        example: /uploads/proofs/proof1.jpg

  /payments/admin/payments:
    get:
      tags:
        - Payments
      summary: List all payments (Admin)
      description: Get all payments with filtering. Admin only.
      security:
        - BearerAuth: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
        - name: status
          in: query
          schema:
            type: string
            enum: [pending, approved, rejected]
        - name: method
          in: query
          schema:
            type: string
            enum: [telebirr, cbe, commercial, abyssinia]
        - name: search
          in: query
          schema:
            type: string
      responses:
        '200':
          description: Payments list retrieved
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: object
                    properties:
                      payments:
                        type: array
                        items:
                          $ref: '#/components/schemas/Payment'
                      total:
                        type: integer
                        example: 100

  /payments/admin/event-payments:
    get:
      tags:
        - Payments
      summary: List event payments (Admin)
      description: Get all event ticket payments. Admin only.
      security:
        - BearerAuth: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
        - name: status
          in: query
          schema:
            type: string
            enum: [pending, approved, rejected]
      responses:
        '200':
          description: Event payments retrieved
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: object
                    properties:
                      payments:
                        type: array
                        items:
                          $ref: '#/components/schemas/Payment'
                      total:
                        type: integer
                        example: 50

  /payments/{id}/process:
    post:
      tags:
        - Payments
      summary: Process payment (Admin)
      description: Approve or reject a payment. Admin only.
      security:
        - BearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          example: 1
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                simulateSuccess:
                  type: boolean
                  description: If true, approves the payment
                  example: true
      responses:
        '200':
          description: Payment processed
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    $ref: '#/components/schemas/Payment'


  # ═══════════════════════════════════════════════════════════════════════════════
  # REPORTS & DASHBOARD
  # ═══════════════════════════════════════════════════════════════════════════════

  /reports/admin/dashboard/metrics:
    get:
      tags:
        - Reports
      summary: Get dashboard metrics (Admin)
      description: Get key metrics for the admin dashboard
      security:
        - BearerAuth: []
      responses:
        '200':
          description: Dashboard metrics retrieved
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: object
                    properties:
                      totals:
                        type: object
                        properties:
                          users:
                            type: integer
                            example: 250
                          bookings:
                            type: integer
                            example: 80
                          revenue:
                            type: number
                            example: 1250000.00
                          gallery:
                            type: integer
                            example: 120
                          activeUsers:
                            type: integer
                            example: 200
                          pendingBookings:
                            type: integer
                            example: 15
                      recentActivity:
                        type: array
                        items:
                          type: object
                          properties:
                            type:
                              type: string
                              example: booking
                            message:
                              type: string
                              example: New booking from Abebe Kebede
                            createdAt:
                              type: string
                              format: date-time

  /reports/admin/reports/bookings-over-time:
    get:
      tags:
        - Reports
      summary: Bookings over time (Admin)
      description: Get booking counts grouped by time period
      security:
        - BearerAuth: []
      parameters:
        - name: groupBy
          in: query
          schema:
            type: string
            enum: [day, week, month]
            default: month
        - name: from
          in: query
          schema:
            type: string
            format: date
        - name: to
          in: query
          schema:
            type: string
            format: date
      responses:
        '200':
          description: Bookings over time data
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        period:
                          type: string
                          example: "2026-04"
                        count:
                          type: integer
                          example: 12

  /reports/admin/reports/revenue-over-time:
    get:
      tags:
        - Reports
      summary: Revenue over time (Admin)
      description: Get revenue data grouped by time period
      security:
        - BearerAuth: []
      parameters:
        - name: groupBy
          in: query
          schema:
            type: string
            enum: [day, week, month]
            default: month
        - name: from
          in: query
          schema:
            type: string
            format: date
        - name: to
          in: query
          schema:
            type: string
            format: date
      responses:
        '200':
          description: Revenue over time data
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        period:
                          type: string
                          example: "2026-04"
                        revenue:
                          type: number
                          example: 125000.00
                        transactions:
                          type: integer
                          example: 8

  /reports/admin/reports/service-distribution:
    get:
      tags:
        - Reports
      summary: Service distribution (Admin)
      description: Get booking counts by event type
      security:
        - BearerAuth: []
      responses:
        '200':
          description: Service distribution data
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        eventType:
                          type: string
                          example: wedding
                        bookings:
                          type: integer
                          example: 35

  /reports/admin/reports/user-growth:
    get:
      tags:
        - Reports
      summary: User growth over time (Admin)
      description: Get new user registrations grouped by time period
      security:
        - BearerAuth: []
      parameters:
        - name: groupBy
          in: query
          schema:
            type: string
            enum: [day, week, month]
            default: month
        - name: from
          in: query
          schema:
            type: string
            format: date
        - name: to
          in: query
          schema:
            type: string
            format: date
      responses:
        '200':
          description: User growth data
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        period:
                          type: string
                          example: "2026-04"
                        count:
                          type: integer
                          example: 25

  /reports/admin/reports/traffic-source:
    get:
      tags:
        - Reports
      summary: Traffic source analytics (Admin)
      description: Get traffic source breakdown
      security:
        - BearerAuth: []
      responses:
        '200':
          description: Traffic source data
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        source:
                          type: string
                          example: direct
                        count:
                          type: integer
                          example: 120
                        percentage:
                          type: number
                          example: 48.0

  /reports/admin/reports/event-stats:
    get:
      tags:
        - Reports
      summary: Event statistics (Admin)
      description: Get event ticket sales and attendance statistics
      security:
        - BearerAuth: []
      responses:
        '200':
          description: Event statistics data
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        eventId:
                          type: integer
                          example: 1
                        title:
                          type: string
                          example: Ethiopian New Year Celebration
                        totalTickets:
                          type: integer
                          example: 200
                        soldTickets:
                          type: integer
                          example: 150
                        revenue:
                          type: number
                          example: 75000.00


  # ═══════════════════════════════════════════════════════════════════════════════
  # ADMIN CONFIG
  # ═══════════════════════════════════════════════════════════════════════════════

  /admin-config/pricing-rules:
    get:
      tags:
        - Admin Config
      summary: Get pricing rules (Admin)
      description: Get all pricing rules for different event types
      security:
        - BearerAuth: []
      responses:
        '200':
          description: Pricing rules retrieved
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: object
                    properties:
                      rules:
                        type: array
                        items:
                          $ref: '#/components/schemas/PricingRule'

  /admin-config/pricing-rules/{eventType}:
    put:
      tags:
        - Admin Config
      summary: Update pricing rule (Admin)
      description: Create or update pricing rule for a specific event type
      security:
        - BearerAuth: []
      parameters:
        - name: eventType
          in: path
          required: true
          schema:
            type: string
            enum: [wedding, birthday, corporate, other]
          example: wedding
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - basePrice
                - perGuest
                - perHour
                - defaultHours
              properties:
                basePrice:
                  type: number
                  format: float
                  example: 10000.00
                perGuest:
                  type: number
                  format: float
                  example: 50.00
                perHour:
                  type: number
                  format: float
                  example: 500.00
                defaultHours:
                  type: integer
                  example: 5
      responses:
        '200':
          description: Pricing rule updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    $ref: '#/components/schemas/PricingRule'

  /admin-config/payment-methods:
    get:
      tags:
        - Admin Config
      summary: Get payment methods config (Admin)
      description: Get configuration for all payment methods
      security:
        - BearerAuth: []
      responses:
        '200':
          description: Payment methods config retrieved
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: object
                    properties:
                      configs:
                        type: array
                        items:
                          $ref: '#/components/schemas/PaymentMethodConfig'

  /admin-config/payment-methods/{method}:
    put:
      tags:
        - Admin Config
      summary: Update payment method config (Admin)
      description: Create or update configuration for a specific payment method
      security:
        - BearerAuth: []
      parameters:
        - name: method
          in: path
          required: true
          schema:
            type: string
            enum: [telebirr, cbe, commercial, abyssinia]
          example: telebirr
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                receiverName:
                  type: string
                  example: Habesha Events
                receiverPhone:
                  type: string
                  example: "0911000000"
                receiverAccountNumber:
                  type: string
                  example: "1000123456789"
                note:
                  type: string
                  nullable: true
                  example: Please include your booking ID in the reference
                active:
                  type: boolean
                  example: true
      responses:
        '200':
          description: Payment method config updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    $ref: '#/components/schemas/PaymentMethodConfig'

  # ═══════════════════════════════════════════════════════════════════════════════
  # NOTIFICATIONS
  # ═══════════════════════════════════════════════════════════════════════════════

  /notifications/admin:
    get:
      tags:
        - Notifications
      summary: Get admin notifications
      description: Get all notifications for admin users
      security:
        - BearerAuth: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
        - name: isRead
          in: query
          schema:
            type: boolean
      responses:
        '200':
          description: Notifications retrieved
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: object
                    properties:
                      notifications:
                        type: array
                        items:
                          $ref: '#/components/schemas/Notification'
                      total:
                        type: integer
                        example: 25

  /notifications/admin/unread-count:
    get:
      tags:
        - Notifications
      summary: Get unread notification count
      description: Get the count of unread notifications for admin
      security:
        - BearerAuth: []
      responses:
        '200':
          description: Unread count retrieved
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: object
                    properties:
                      count:
                        type: integer
                        example: 5

  /notifications/admin/{id}/read:
    patch:
      tags:
        - Notifications
      summary: Mark notification as read
      description: Mark a specific notification as read
      security:
        - BearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
          example: 1
      responses:
        '200':
          description: Notification marked as read
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse'

  /notifications/admin/read-all:
    patch:
      tags:
        - Notifications
      summary: Mark all notifications as read
      description: Mark all admin notifications as read
      security:
        - BearerAuth: []
      responses:
        '200':
          description: All notifications marked as read
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse'

  # ═══════════════════════════════════════════════════════════════════════════════
  # DASHBOARD (Legacy endpoints)
  # ═══════════════════════════════════════════════════════════════════════════════

  /dashboard/admin:
    get:
      tags:
        - Dashboard
      summary: Get admin dashboard (Legacy)
      description: Legacy endpoint - use /reports/admin/dashboard/metrics instead
      security:
        - BearerAuth: []
      responses:
        '200':
          description: Dashboard data retrieved
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: object

  /dashboard/jobseeker:
    get:
      tags:
        - Dashboard
      summary: Get jobseeker dashboard (Legacy)
      description: Legacy endpoint - may not be used in current frontend
      security:
        - BearerAuth: []
      responses:
        '200':
          description: Dashboard data retrieved
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: object

  /dashboard/employer:
    get:
      tags:
        - Dashboard
      summary: Get employer dashboard (Legacy)
      description: Legacy endpoint - may not be used in current frontend
      security:
        - BearerAuth: []
      responses:
        '200':
          description: Dashboard data retrieved
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: object
